// 扩展 expect 匹配器（可选，需安装 @testing-library/jest-dom）
//import '@testing-library/jest-dom/vitest';

// 可选：全局配置 Vue 测试工具
import { config } from '@vue/test-utils';
// 例如：配置全局组件
// config.global.components = { MyGlobalComponent };